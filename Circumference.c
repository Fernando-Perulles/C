#include <stdio.h>
#define PI 3.14 // Definición del valor que se ocupara para PI.

int main() {
	
	float r = 25;
	float c = PI * (r * r);
	
	printf("Radius Value : %f\n", r);
	printf("Circumference Value : %f\n", c);
	
	return 0;
}
